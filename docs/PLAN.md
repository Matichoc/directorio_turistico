# El diablo murió en Petorca y en La Ligua lo enterraron — Plan del proyecto

> Documento vivo. Actualizar al cerrar cada fase o al tomar una decisión técnica relevante.

## Estado actual

- Fase: **Fase 1 — completada** (mergeada a `main`). Proyecto Supabase real ya creado por el usuario, migraciones y seed aplicados y verificados en vivo.
- Última actualización: 2026-09-26
- Lineamientos visuales (colores, radios, animaciones, íconos, patrones de componente) viven en `docs/DESIGN.md` — se carga automático en cada sesión de Claude Code vía `CLAUDE.md`, para que una página o componente nuevo siga la misma línea sin tener que pedirlo cada vez.

## 1. Arquitectura

Aplicación única Next.js (App Router), no monorepo.

| Capa del encargo         | Implementación                                                                         |
| ------------------------ | -------------------------------------------------------------------------------------- |
| Interfaz pública         | `app/[locale]/...`                                                                     |
| Panel administrativo     | `app/admin/...`, protegido con Supabase Auth                                           |
| Acceso a datos           | `lib/data/*`                                                                           |
| Mapas/geocodificación    | `lib/maps/*` (MapLibre GL, proveedor de tiles por env var)                             |
| Motor de itinerarios     | `lib/itinerary-engine/*` — módulo TS puro, sin dependencias de UI ni Supabase directas |
| Contenido y verificación | Server actions en `lib/server/content/*` + `sources`, `verification_logs`              |
| Analítica                | `lib/analytics/*`, eventos anónimos                                                    |
| Futuro conversacional    | Interfaz `ItineraryEngine.build(input): Itinerary` como punto de extensión             |

### Decisiones técnicas (por defecto, reversibles)

- **i18n:** `next-intl` (rutas localizadas `/es`, `/en`, mensajes separados, hreflang).
- **PWA:** `Serwist` en vez de `next-pwa` (mejor mantenido, compatible con App Router). _Cambio respecto a lo implícito en el encargo original — señalado al usuario._
- **Mapas:** MapLibre GL JS + `react-map-gl`, proveedor de tiles configurable por env var (`NEXT_PUBLIC_MAP_STYLE_URL`). Proveedor comercial definitivo: **pendiente de definir antes de producción** (implica costos).
- **Validación:** `zod`.
- **Testing:** `vitest` + Testing Library + Playwright.
- **Gestor de paquetes:** `pnpm`.
- **Fechas:** `date-fns` + `date-fns-tz`.
- **Base de datos:** Supabase (Postgres + PostGIS + Auth + Storage). RLS deny-by-default en itinerarios; ver `docs/DATA-MODEL.md`.

## 2. Estructura de carpetas

```
directorio_turistico/
├── src/
│   ├── app/
│   │   ├── [locale]/
│   │   │   ├── page.tsx
│   │   │   ├── explorar/ (en: explore)
│   │   │   ├── rutas/[slug]/ (en: routes)
│   │   │   ├── lugares/[slug]/ (en: places)
│   │   │   ├── recorrido/ (en: my-trip)
│   │   │   └── informacion/ (en: info)
│   │   ├── admin/
│   │   │   ├── lugares/
│   │   │   ├── rutas/
│   │   │   ├── verificaciones/
│   │   │   ├── comunas-categorias/
│   │   │   └── login/
│   │   ├── api/
│   │   │   └── analytics/
│   │   └── manifest.ts / sw.ts
│   ├── components/
│   │   ├── ui/
│   │   ├── map/
│   │   ├── place/
│   │   ├── route/
│   │   └── itinerary/
│   ├── lib/
│   │   ├── data/
│   │   ├── itinerary-engine/
│   │   ├── maps/
│   │   ├── analytics/
│   │   ├── server/content/
│   │   └── supabase/
│   ├── i18n/
│   ├── messages/
│   │   ├── es.json
│   │   └── en.json
│   └── types/
├── supabase/migrations/
├── public/
├── tests/{unit,integration,e2e}/
├── scripts/
├── docs/{ARCHITECTURE.md,PLAN.md,DATA-MODEL.md}
├── .env.example
└── README.md
```

## 3. Esquema de base de datos (resumen)

**Catálogo/geografía:** communes, commune_translations, localities, categories, category_translations, tags
**Lugares:** places, place_translations, place_hours, place_images, place_tags
**Rutas:** routes, route_translations, route_stops, route_stop_translations
**Verificación:** sources, verification_logs
**Itinerarios:** itineraries, itinerary_stops
**Analítica:** analytics_events
**Auth/admin:** admin_users (mapea `auth.users` a rol de administrador)

Ver detalle completo en `docs/DATA-MODEL.md` y `supabase/migrations/*.sql`.

Reglas: único `(entity_id, locale)` en cada tabla `*_translations`; columna `geography(Point,4326)` generada + índice GiST en places/localities; índice trigram en nombre/descripción; RLS por estado de publicación y por rol (`public.is_admin()`).

## 4. Dependencias principales

next, react, typescript, tailwindcss, next-intl, @supabase/supabase-js, @supabase/ssr, maplibre-gl, react-map-gl, zod, @serwist/next, serwist, date-fns, date-fns-tz, vitest, @testing-library/react, @playwright/test, eslint, prettier.

## 5. Riesgos

1. Costo/cuota del proveedor de tiles — definir antes de producción. Mientras tanto se usa el estilo demo gratuito de MapLibre.
2. Desincronización ES/EN si el marcado "requiere revisión" (`place_translations.needs_review`) falla.
3. Soporte parcial de PWA en iOS Safari — documentar limitaciones.
4. Complejidad del motor de itinerarios (coherencia geográfica, horarios) — requiere pruebas unitarias exhaustivas antes de UI. La Fase 0 entrega una versión base (vecino más cercano) con tests; falta soporte de horarios de apertura.
5. RLS debe cubrir tablas base y tablas de traducción — cubierto en `0008_rls.sql`, pendiente de probar contra datos reales.
6. Gobernanza de traducciones IA — decisión pendiente para Fase 4.
7. ~~Aunque el seed usa lugares/rutas reales...~~ — resuelto: cada lugar/ruta del seed cita una fuente pública en `sources` (Wikipedia, Sernageomin, municipios, sitio oficial de Matichoc, etc.), así que `scripts/seed.ts` los marca `verification_status = 'verified'` y la ficha muestra el badge "Verificado". Sigue pendiente la verificación en terreno propiamente tal (horarios exactos, datos de contacto de algunos lugares) — el admin puede bajar el estado a `pending`/`outdated` si detecta un error.
8. Íconos PWA (`public/icons/icon-192.png`, `icon-512.png`) son placeholders pendientes de diseño final.
9. ~~Sin proyecto Supabase real conectado~~ — resuelto: el usuario creó el proyecto (`diablo_petorca_ligua`), aplicó las migraciones vía CLI y corrió el seed.
10. Sandbox de esta sesión sin salida de red hacia `supabase.co` (política de egress de la organización): no se pudo probar visualmente contra la base real desde el entorno de Claude Code; toda validación en vivo la hizo el usuario en su máquina. Si esto se repite, delegar la verificación visual al usuario o a CI con secretos propios.
11. `listPlaces`/`listRoutes` filtran `tagSlug`/`query` en memoria después de traer todos los lugares publicados (ver comentario en `lib/data/places.ts`) — válido mientras el catálogo sea pequeño (decenas de lugares); si crece, mover a filtros SQL/`!inner` dinámicos o paginar.
12. El selector de intención del home usa las 4 categorías reales del seed (naturaleza, gastronomía, cultura, playa) — se ajustó `home.intent` de "aventura" a "playa" para que coincida con datos reales en vez de una categoría inexistente.
13. El carrito de recorrido (`lib/trip/storage.ts`) vive solo en `localStorage` del navegador, sin sesión de servidor — es la interpretación pragmática de las tablas `itineraries`/`itinerary_stops` del esquema (aún sin poblar) mientras no exista auth de sesión anónima. Migrar a esas tablas cuando se implemente sesión anónima real.
14. La galería de fotos de cada lugar (`place_images` en el esquema) sigue mayormente sin poblar. Se cargaron 4 fotos reales: 3 con licencia libre desde Wikimedia Commons (Museo de La Ligua, Plaza de Armas de La Ligua, Iglesia La Merced de Petorca — ver `scripts/seed.ts`, `wikimediaFilePath()`) vía `Special:FilePath`, y 1 foto real de producto de Chocolatería Matichoc copiada desde el repo `matichoc/matiweb` a `public/fotos/chocolateria-matichoc/` (archivo estático local, servido directo por Next.js — más simple y confiable que hotlinkear un dominio externo variable). El resto de los lugares (34 en total tras sumar los de abajo) sigue mostrando la cabecera con gradiente por categoría. `place_images.storage_path` se está usando como URL/path completo (externo o `/fotos/...` local), no como path real de Supabase Storage — atajo pragmático mientras no exista un pipeline de subida propio. Existe `public/fotos/<slug>/` (con README) para que el usuario pegue fotos propias — una carpeta por cada lugar del catálogo.
15. `maplibre-gl` está fijado a `^5.24.0` en `package.json` a propósito (no subir a v6 sin más trabajo): la v6 cambió cómo se cargan los workers/tiles y el mapa queda completamente en blanco con `react-map-gl`, sin ningún error en consola — bug real detectado en esta sesión (el usuario probó en su máquina y ningún mapa se veía) y reportado por varios proyectos del ecosistema. Antes de subir de versión, seguir la guía de migración de MapLibre para configurar el worker con el bundler de Next.js/Turbopack y volver a probar visualmente.
16. El estilo de mapa se cambió de "positron" (gris/minimalista) a "liberty" (colores vivos) a pedido del usuario ("mapas... poco interactivos, se ven plomos feos"); además se rediseñaron los pines (`components/map/map-pin.tsx`): forma de gota con color e ícono por categoría, halo pulsante y animación de aparición. Sigue pendiente cualquier decoración temática adicional (mar/cordillera/diablo) que el usuario pida tras ver este cambio.
17. El zoom del mapa estaba fijo en `PETORCA_DEFAULT_ZOOM` (10, toda la provincia) para cualquier vista, incluida la ficha de un solo lugar — se veía "genérico" sin mostrar la ubicación real. Se corrigió: un solo marcador usa `PLACE_DETAIL_ZOOM` (15, nivel calle); 2+ marcadores usan `fitBounds` para encuadrar la extensión real de los lugares en vez de un zoom fijo.
18. Varios lugares del seed compartían exactamente las mismas coordenadas (el centro del pueblo/comuna usado como aproximación): Museo/Plaza/Matichoc en La Ligua, Iglesia/Casa natal Manuel Montt en Petorca, Pedegua/Ruta de los Túneles en Cabildo. Se separaron con pequeños ajustes manuales (nudges) documentados en `scripts/seed.ts` — siguen siendo aproximados, no geocodificación exacta; de paso se corrigió la dirección de la Casa natal de Manuel Montt (845, no 835) con una fuente más autorizada (Consejo de Monumentos Nacionales).
19. El usuario pasó coordenadas GPS exactas (tomadas de Google Maps) para 5 lugares existentes cuyas coordenadas eran nudges/aproximaciones nuestras (Museo de La Ligua, Plaza de Armas de La Ligua, Iglesia La Merced de Petorca, Escalera del Diablo, Pedegua) — se reemplazaron por las exactas, y `ruta-de-los-tuneles` se reubicó en el punto de acceso real (Pedegua). Además pidió sumar ~23 lugares/sectores nuevos con coordenadas exactas, ya investigados y creados en `scripts/seed.ts`: Valle Hermoso, Área de los Dulces, La Chorreada, Los Molles y Pichicuy (La Ligua); Salinas de Pullally, Playa Los Lilenes, Costanera, Plaza y Paseo El Conquistador (Papudo); Laguna, Cachagua/Las Cujas y Catapilco/Palos Quemados (Zapallar); Plaza, Alicahue, San Lorenzo, La Vega y Túnel La Grupa (Cabildo); Puente Pedegua, Túnel Las Palmas, Plaza y Petroglifos de Chincolco (Petorca). Notas: (a) "La Chorreada" no tiene fuente sólida encontrada — descripción honesta y mínima, fuente débil (Instagram); (b) Túnel Las Palmas queda geográficamente en la comuna de Los Vilos (Región de Coquimbo), no en la provincia de Petorca — se asignó a la comuna Cabildo de nuestro catálogo por ser parte del mismo corredor ferroviario histórico, documentado en el código.
20. ~~Cobertura de fotos reales para los ~30 lugares sin foto curada a mano~~ — resuelto en su mayoría: el usuario eligió Google Places API (New) (Text Search + Place Photo media) en vez de Unsplash (conector encontrado incompleto/sin fotos) o seguir buscando en Wikimedia Commons. `GOOGLE_PLACES_API_KEY` nunca se expone al navegador: `src/app/api/place-photo/route.ts` hace streaming del binario server-side, y `scripts/fetch-google-photos.ts` (que corre el usuario localmente, con su propio proyecto de Google Cloud/GSuite con facturación habilitada) busca y guarda solo en lugares que **no** tenían ya una foto en `place_images` — esto protegió automáticamente las 4 fotos curadas a mano sin necesitar una lista de exclusión. El usuario lo corrió y confirmó: 26 fotos agregadas, 4 lugares sin resultado en Google (probablemente sectores/localidades menores sin ficha propia en Google Maps, no lugares con nombre incorrecto — pendiente de identificar cuáles si el usuario quiere cubrirlos a mano) y 0 errores. Bug real encontrado al probar en vivo: `/explorar`, `/` y la ficha de cualquier lugar con foto nueva de Google tiraban `Error: Image with src "/api/place-photo?ref=...&w=..." is using a query string which is not configured in images.localPatterns` (500 en la ficha, error boundary genérico en las demás) — cambio de breaking en Next 16 (`AGENTS.md`/`node_modules/next/dist/docs/01-app/02-guides/upgrading/version-16.md`): las imágenes locales (mismo origen) con query string ahora requieren declarar `images.localPatterns` en `next.config.ts`, igual que `remotePatterns` para dominios externos, para evitar ataques de enumeración. Se agrega `localPatterns: [{ pathname: "/api/place-photo" }]` (sin `search`, para permitir cualquier query en esa ruta — la propia ruta ya valida `ref` y acota `w` antes de llamar a Google). Segundo bug relacionado, detectado también en vivo: declarar `localPatterns` convierte a `next/image` de "permite cualquier imagen local sin query string" a "solo permite lo listado ahí" — rompió la foto local de Chocolatería Matichoc (`/fotos/chocolateria-matichoc/tableta.jpg`, que no tiene query string pero tampoco estaba en la lista). Se agrega también `{ pathname: "/fotos/**", search: "" }`. Además se achica la cabecera de foto de `aspect-ratio` (crecía junto al ancho de pantalla, se veía "gigante" en monitores anchos aunque la foto fuera real y estuviera bien encuadrada — feedback repetido) a un alto fijo (`h-48 sm:h-64`). Confirmado visualmente por el usuario: la foto real de Playa de Papudo carga y se ve bien encuadrada.
21. Nuevo: monetización vía posiciones publicitarias. El usuario quiere vender (a) espacio de banner (hoy solo Matichoc, hardcodeado por env var) y (b) destacar lugares del catálogo en el listado — cobro gestionado por fuera del sitio, sin pasarela de pago. Se agregó el mecanismo base (`places.featured_until`, insignia "Destacado") pero **no** una forma de autogestionarlo: el panel admin (`/admin/lugares`) sigue siendo un placeholder de Fase 2, así que activar/desactivar un destacado hoy requiere editar `scripts/seed.ts` y correr `pnpm db:seed` — viable mientras haya pocos auspiciadores, no escala a un panel de ventas real. Falta también soportar más de un banner/auspiciador (el componente `SponsorBanner` sigue siendo un solo slot fijo). Pendiente de decidir con el usuario cuándo construir un admin CRUD real para esto.
22. Mapa de navegación en vivo: requiere que el usuario habilite "Routes API" en el mismo proyecto de Google Cloud que ya usa para Places (mismo `GOOGLE_PLACES_API_KEY`) — sin eso, `/api/directions` responde 503 y el mapa cae al modo normal sin romperse, pero sin línea de ruta ni resumen de viaje. Geolocalización del navegador requiere HTTPS en producción (funciona en `localhost` para desarrollo). No se probó en vivo desde este sandbox (sin salida de red hacia `routes.googleapis.com`, igual que con Places) — falta confirmación visual del usuario, incluida la aparición real del punto de ubicación al mover el dispositivo.

## 6. Fase 0 — tareas

- [x] Crear repositorio y README inicial
- [x] Inicializar Next.js + TS + Tailwind + ESLint/Prettier
- [x] Configurar pnpm y scripts (dev/build/lint/test/test:e2e)
- [x] Crear .env.example documentado
- [x] Configurar clientes Supabase (browser/server/admin)
- [x] Migraciones iniciales (todas las tablas)
- [x] Políticas RLS base
- [x] Seed: 5 comunas, categorías/tags, 9 lugares reales (con fuente en `sources`) y 3 rutas — incluida "Ruta del Diablo" —, todos pendientes de verificación
- [x] Configurar next-intl (es/en, middleware de locale)
- [x] Configurar Vitest + Testing Library + Playwright (esqueleto)
- [x] Documentar arquitectura y este plan
- [ ] Crear proyecto Supabase real y aplicar migraciones (requiere acción del cliente/usuario)

## 7. Fase 1 — tareas

- [x] Layout raíz mobile-first + navegación inferior
- [x] Middleware de locale (/ → /es, selector ES|EN persistente)
- [x] Portada (hero, buscador → `/explorar?q=`, selector de intención → `/explorar?categoria=`, rutas destacadas reales, acceso a "Ver todas las rutas")
- [x] Explorador: listado de tarjetas + mapa (MapLibre, marcadores + popup) + toggle vía `?vista=mapa|lista`
- [x] Filtros (comuna, categoría, características) vía `lib/data/communes.ts`, `categories.ts`, `tags.ts` + `ExploreFilters` (query params en la URL, compartibles)
- [x] Buscador por nombre/localidad (`?q=`, filtra en memoria sobre nombre/comuna/descripción corta)
- [x] Ficha de lugar completa (dirección, teléfono, sitio web, tags, mapa embebido en la misma página, compartir con Web Share API + fallback a copiar enlace, badge de verificación, botón "agregar a mi recorrido")
- [x] Ficha de ruta completa (duración, lista de paradas enlazadas a cada lugar, mapa embebido con todas las paradas, botón "agregar ruta completa" al recorrido)
- [x] Recorrido (`/recorrido`): carrito de lugares en `localStorage`, arma el itinerario real vía `ItineraryEngine.build` (orden por vecino más cercano, distancia y tiempo estimados), mapa con las paradas ordenadas
- [x] Estados de carga (`loading.tsx` con skeletons en explorar/rutas), error (`error.tsx` genérico en `[locale]`) y sin resultados (`EmptyState`)
- [x] Accesibilidad WCAG AA básica: labels asociados a inputs/selects, `aria-pressed` en el toggle de vista, `aria-label` en marcadores del mapa, `role="search"`/`role="status"`, foco visible por defecto del navegador (sin `outline-none`)
- [x] Pruebas de integración para `PlaceCard`/`RouteCard` (se corrigió un bug real: faltaba `cleanup()` entre tests de Testing Library en `tests/setup.ts`, causaba falsos positivos/negativos por acumulación del DOM)
- [ ] `pnpm test:e2e` (Playwright) contra datos reales — no ejecutado en esta sesión (sandbox sin acceso de red a Supabase)

## Bitácora de decisiones

- 2026-09-21: Propuesta inicial de arquitectura, esquema y fases 0-1 presentada, pendiente de aprobación.
- 2026-09-22: Usuario aprueba ejecutar la Fase 0 completa. Sin proyecto Supabase real disponible (el usuario no tiene uno creado): se escriben migraciones, RLS y seed listos para aplicar, pero no se conecta a ningún proyecto en vivo. Scaffolding de Next.js 16 + next-intl + Supabase clients + itinerary-engine + testing completado. PR #1 mergeado a `main`.
- 2026-09-22: Cambio de marca — el sitio pasa a llamarse "El diablo murió en Petorca y en La Ligua lo enterraron" (dicho popular chileno sobre la rivalidad Petorca/La Ligua, documentado desde 1894). "Petorca en Ruta" queda solo como nombre de trabajo interno del repo/paquete npm. Matichoc deja de ser la marca del sitio: pasa a auspiciador, con un banner discreto (`components/ui/sponsor-banner.tsx`) en vez de branding. Se reemplazan los lugares/rutas demo por 9 lugares y 3 rutas reales de la provincia (Playa de Papudo, Playa Chica, Bahía y mirador de Zapallar, Museo y Plaza de La Ligua, Escalera del Diablo, Iglesia La Merced y casa natal de Manuel Montt en Petorca, Cerro Chache en Cabildo), investigados vía web search con fuente citada en la tabla `sources` para cada uno — incluida una "Ruta del Diablo" que conecta Hierro Viejo, Petorca y La Ligua siguiendo el dicho. Quedan `publication_status = 'published'` (para que se vean apenas se apliquen el seed) pero `verification_status = 'pending'`: son datos reales pero no verificados en terreno.
- 2026-09-22: Usuario prueba la app localmente (`pnpm dev` en Windows) — confirma que el home, el banner y la navegación funcionan, pero `/es/rutas` y `/es/explorar` solo muestran el esqueleto de Fase 0 (sin listado de tarjetas) y no hay datos porque aún no existe un proyecto Supabase real. Se corrige el seed para publicar el contenido (antes quedaba en `draft`, invisible incluso una vez cargado). Usuario pide ayuda para crear el proyecto Supabase paso a paso.
- 2026-09-22: Usuario crea el proyecto Supabase (`diablo_petorca_ligua`), aplica las 8 migraciones vía CLI (`supabase db push`) y corre el seed (`pnpm db:seed`), confirmado en vivo: ficha de "Escalera del Diablo" y de "Ruta del Diablo" cargan con datos reales y el badge "pendiente de verificación". Nota: `pnpm db:seed` fallaba porque el script no cargaba `.env.local` (a diferencia de `next dev`, un script suelto con `tsx` no pasa por la carga automática de env de Next.js) — se agrega `dotenv` para cargarlo explícitamente.
- 2026-09-22: Usuario aprueba avanzar con la Fase 1 completa. Se implementa: capa de datos (`listPlaces`, `listRoutes`, `listCommunes`, `listCategories`, `listTags`), componentes (`PlaceCard`, `RouteCard`, `ExploreFilters`, `ViewToggle`, `MapView` con marcadores/popup, `ShareButton`, `HomeSearchForm`, `EmptyState`), páginas completas (home, explorar con filtros+mapa, listado de rutas, fichas de lugar/ruta), estados de carga/error, y tests de integración. No se pudo probar visualmente desde el sandbox de esta sesión (sin egress de red hacia `supabase.co`, política de la organización) — el usuario compartió credenciales públicas (URL + publishable key) pero el build/typecheck/lint/tests corrieron localmente sin problema; falta que el usuario confirme visualmente en su máquina.
- 2026-09-22: PR de Fase 1 mergeado y confirmado por el usuario. Pide interacción completa "en la misma web" (sin salir a Google Maps), el link real de Matichoc (matichoc.cl), datos marcados como validados, opción de armar itinerario, banner de auspicio más visual, y mejor UI/UX general. Se investigan y suman 3 lugares reales más (pedido explícito del usuario): Chocolatería Matichoc (La Ligua, con dirección/teléfono/sitio real de matichoc.cl), Pedegua (Cabildo, antigua estación de ferrocarril) y Ruta de los Túneles (línea férrea Cabildo–Pedegua con 5 túneles y 2 puentes, incluido el túnel Las Palmas de 1914) — cada uno con fuente pública citada en `sources`; coordenadas sin geocodificación exacta se calcularon desde la referencia UTM publicada por la fuente. Todos los lugares/rutas del seed pasan de `verification_status = 'pending'` a `'verified'` (tienen fuente citada). Se implementa: mapa embebido (MapLibre + OpenFreeMap, estilo "positron" gratuito sin API key, reemplaza el estilo demo anterior) en fichas de lugar/ruta en vez de link externo a Google Maps; carrito de recorrido en `localStorage` (`lib/trip/storage.ts`) con botones "agregar a mi recorrido"/"agregar ruta completa" que emiten los eventos de analítica ya definidos (`place_added_to_trip`/`route_added_to_trip`); página `/recorrido` que arma el itinerario real vía `ItineraryEngine.build` (ya existía el motor puro, sin UI) mostrando orden, distancia y tiempo estimados sobre un mapa; banner de Matichoc rediseñado (tarjeta visual con logo/gradiente, link real a matichoc.cl, Instagram opcional solo si se configura — no se inventó una cuenta sin verificar); paleta de acento (terracota) + iconos SVG propios en nav inferior y categorías; rediseño visual de home (hero con gradiente, selector de intención como tiles con ícono), tarjetas de lugar/ruta y filtros. Bug real encontrado y corregido en el camino: `/rutas/[slug]` devolvía HTTP 200 en vez de 404 para una ruta inexistente — causado por `rutas/loading.tsx` (Suspense a nivel de segmento) haciendo que Next.js comprometa el status 200 al empezar a transmitir el skeleton antes de que `notFound()` se resuelva; se corrigió moviendo el listado (`rutas/page.tsx` + `loading.tsx`) a un route group `rutas/(list)/` que no envuelve a `rutas/[slug]`, sin cambiar las URLs. `pnpm typecheck`/`lint`/`test`/`build` verdes; smoke test con `curl` contra el dev server confirma códigos HTTP correctos en todas las rutas nuevas y existentes (imposible probar contra datos reales por el mismo bloqueo de red de siempre — falta confirmación visual del usuario).
- 2026-09-22: Usuario prueba localmente y reporta mapas "poco interactivos... plomos feos" y "no muestra bien los lugares... muestra algo genérico"; pide más "dinamismo" y ofrece habilitar herramientas si hace falta. Se detecta y corrige un bug real: `maplibre-gl` estaba en v6, que rompe la carga de tiles con `react-map-gl` sin ningún error visible (bug conocido del ecosistema) — se fija a `^5.24.0`. Se cambia el estilo de mapa de "positron" a "liberty" (más colorido) y se rediseñan los pines (forma de gota, color/ícono por categoría, halo pulsante, animación de aparición). Se corrige el zoom fijo en vista de toda la provincia (se veía "genérico" en la ficha de un lugar): ahora un solo marcador usa zoom de nivel calle y 2+ marcadores usan `fitBounds`. Se detectan y corrigen varios lugares con coordenadas idénticas (apiladas en un mismo pin) con nudges manuales documentados. Usuario manda captura mostrando una foto real de Wikimedia Commons (Iglesia La Merced) recortada casi todo cielo — causa: franja de altura fija con `object-cover` sobre una foto en formato retrato; se corrige a `aspect-ratio` responsivo + `object-position` sesgado hacia abajo. Usuario conecta Unsplash pero prefiere fotos reales; comparte el repo `matichoc/matiweb` para fotos de Matichoc (se usa `tableta.jpg`, foto real de producto, copiada a `public/fotos/chocolateria-matichoc/`) y pide una carpeta para ir pegando fotos de ~23 lugares/sectores nuevos — se crea `public/fotos/<slug>/` con README. Usuario pasa coordenadas GPS exactas (de Google Maps) para 5 lugares existentes (reemplazando nudges/aproximaciones) y para los ~23 lugares/sectores nuevos, todos investigados y creados en el seed con fuente citada (Wikipedia, municipios, Consejo de Monumentos Nacionales, Sitrural, etc.) — el catálogo pasa de 12 a 34 lugares. `pnpm typecheck`/`lint`/`test`/`build`/`format:check` verdes en cada entrega; como siempre, no se pudo confirmar visualmente desde este sandbox (sin egress hacia `supabase.co`) — falta que el usuario corra `pnpm db:seed` y confirme en su máquina.
- 2026-09-22: Usuario reporta que las imágenes se ven "muy grandes" y no se ven bien en pantalla. Causa real: las fotos se mostraban con `<img>` plano, que descarga el archivo a tamaño completo (varios MB en una foto de celular sin comprimir) y lo achica solo con CSS — pesado y desproporcionado, sobre todo en mobile. Se migra `PhotoOrIcon` a `next/image` (`fill` + `sizes` por contexto: 100vw en la ficha de lugar, grilla responsiva en las tarjetas), que sirve un tamaño acorde a la pantalla del visitante y optimiza el formato — verificado en el sandbox: la misma foto de Matichoc baja de 132 KB a 44 KB en la variante w=640. Se agrega `commons.wikimedia.org` a `images.remotePatterns` en `next.config.ts` para que el optimizador de Next.js pueda procesar también las fotos hotlinkeadas de Wikimedia Commons. De paso se reduce el alto de la cabecera de foto en mobile (`aspect-[4/3]` → `aspect-[3/2]`): con el `aspect-ratio` más alto de la sesión anterior (para no recortar fotos verticales) la foto terminaba dominando la pantalla en mobile.
- 2026-09-22: Usuario pregunta si se pueden usar directo las fotos que muestra Google Maps en las coordenadas dadas. Se explica que no es posible sin API key propia + facturación (ToS de Google prohíbe scraping/rehosting permanente). El usuario responde "hagamos lo más económico y woow posible" — se evalúa Unsplash primero (gratis, ya lo tenía "conectado"), pero `ListConnectors` muestra el conector en estado `connect_incomplete`/sin fotos reales; el usuario corrige y pide seguir con Google Places, indicando que tiene cuenta GSuite propia para habilitar facturación. Se investiga el pricing vigente (2026): el crédito universal de US$200/mes se retiró en 2025, reemplazado por cuotas gratis mensuales por función (Places: ~5.000-10.000 llamadas gratis/mes) — para 34 lugares el costo esperado es US$0, pero igual requiere tarjeta asociada. Usuario pide guía paso a paso para Google Cloud Console. Se construye la integración: `src/app/api/place-photo/route.ts` (proxy que hace streaming de la foto usando `GOOGLE_PLACES_API_KEY` server-side, nunca expuesta al navegador, con `Cache-Control` corto) y `scripts/fetch-google-photos.ts` (script que el usuario corre localmente — el sandbox no tiene salida de red hacia Google — que busca vía Text Search el lugar más cercano a las coordenadas de cada `place` sin foto en `place_images` y guarda la referencia de la primera foto encontrada con su atribución; nunca toca lugares que ya tienen foto, protegiendo así automáticamente las 4 curadas a mano sin lista de exclusión hardcodeada). Se agrega `pnpm fetch:google-photos` y `GOOGLE_PLACES_API_KEY` a `.env.example`. `pnpm typecheck`/`lint`/`test`/`build`/`format:check` verdes. PR mergeado. El usuario reporta que la ficha se veía "gigante" y sin foto en lugares que todavía no tenían ninguna (esperado hasta correr el script) — se ajusta `PlacePhotoHero` para usar una altura fija chica en vez de `aspect-ratio` cuando no hay foto real, en vez del mismo alto que una foto real. El usuario completa el setup de Google Cloud y corre `pnpm fetch:google-photos`: 26 fotos agregadas, 4 lugares sin resultado en Google, 0 errores — confirmado por el usuario, falta solo verificación visual de cómo se ven las fotos nuevas en la ficha.
- 2026-09-22: Usuario prueba en vivo y reporta `/explorar`, `/` y varias fichas rotas con `images.localPatterns`. Se corrige en dos rondas (ver PR #13): falta declarar `/api/place-photo` (query string) y, al agregar esa entrada, se rompe la foto local de Matichoc porque declarar `localPatterns` pasa de "toda imagen local sin query permitida" a "solo lo listado" — se agrega también `/fotos/**`. Además se cambia `PlacePhotoHero` de `aspect-ratio` a alto fijo (`h-48 sm:h-64`): con aspect-ratio el alto crecía junto al ancho de pantalla y en ventanas de escritorio anchas la foto se veía "gigante" aunque fuera una foto real bien encuadrada. El usuario notaba el mismo problema probando en una ventana de Chrome de escritorio sin emular mobile — se le indica usar el modo dispositivo de DevTools (Ctrl+Shift+M) ya que el sitio es mobile-first sin ancho máximo en desktop. Confirmado: Playa de Papudo carga con foto real bien encuadrada. De paso, el usuario pide ajustes de contenido a dos rutas existentes: "Ruta Costera: Papudo y Zapallar" suma Los Molles y Pichicuy (tramo costero de La Ligua) como paradas; "Ruta del Diablo" se reordena para partir en Pedegua (estación y puente — "donde el diablo perdió el poncho") y terminar en los petroglifos de Chincolco, pasando por la Escalera del Diablo y la Iglesia La Merced de Petorca (se sacan Casa natal de Manuel Montt y Museo de La Ligua de las paradas, no mencionadas en la ruta pedida). `pnpm typecheck`/`lint`/`test`/`build`/`format:check` verdes en cada entrega.
- 2026-09-22: Usuario pide empezar a planificar la próxima versión: (1) más de una foto por lugar (4-5, sin selector de portada todavía) y (2) "dinámicas" para las rutas — checklist de paradas, efecto visual al seleccionar la ruta, animaciones, destacar comida en categoría gastronomía, y un mapa interactivo en movimiento tipo Waze/Google Maps. Se implementa (1): `scripts/fetch-google-photos.ts` reescrito para completar cada lugar hasta `TARGET_PHOTOS_PER_PLACE = 5` fotos (antes solo guardaba 1), administrando únicamente las filas cuyo `storage_path` empieza con el prefijo del proxy de Google (se identifican así, no por lista de slugs) — en cada corrida las reemplaza por una búsqueda fresca, y nunca toca ni cuenta las fotos curadas a mano (Wikimedia/`/fotos/` local), que siguen en posición 0 con las de Google agregándose después. El punto 2 queda pendiente de priorizar con el usuario antes de construir (ver mensaje siguiente): el mapa tipo Waze es un desarrollo grande aparte de las demás piezas (checklist, animaciones, etc.), que son más chicas e independientes entre sí. Usuario elige "primero lo chico, mapa después". Se implementan las piezas chicas: checklist de paradas marcables por ruta (`lib/route-progress/storage.ts`, mismo patrón que el carrito de recorrido — solo `localStorage`, sin backend), con barra de progreso, halo pulsante (`animate-route-glow`) en la próxima parada por visitar, y mensaje de felicitación al completar; animaciones de entrada escalonada (`animate-stop-in`) y rebote al marcar (`animate-check-pop`) en `globals.css`; brillo/glow en `RouteCard` al pasar el mouse; insignia de categoría (ícono + nombre, color por categoría) sobre la foto en `PlaceCard`/`PlacePhotoHero` para que gastronomía (y las demás) se distingan a simple vista. Bug real encontrado y corregido de paso: `CategoryIcon` recibía el _slug_ de categoría (`"gastronomia"`) donde esperaba un _nombre de ícono_ (`"utensils"`) en `map-pin.tsx`, `map-view.tsx` y `photo-or-icon.tsx` — como ninguna coincidía, siempre mostraba el ícono por defecto (landmark) sin importar la categoría real; se agrega `getCategoryIcon()` en `lib/ui/category-gradient.ts` y se corrigen los 3 llamadores. `pnpm typecheck`/`lint`/`test`/`build`/`format:check` verdes.
- 2026-09-22: Usuario prueba de nuevo y reporta que no ve más fotos ni las rutas actualizadas — pendiente de que corra `pnpm fetch:google-photos` y `pnpm db:seed` localmente (los cambios de la sesión anterior no se aplican solos a su base Supabase). De paso pide más contenido en las rutas: "Ruta Costera: Papudo y Zapallar" suma Salinas de Pullally (Papudo); "Ruta Patrimonial: La Ligua y Cabildo" se reemplaza por completo — de plaza/museo de La Ligua pasa a La Chorreada (La Ligua) + Cerro Chache, San Lorenzo, Alicahue y La Vega (interior cordillerano de Cabildo). `pnpm typecheck`/`lint`/`test`/`format:check` verdes.
- 2026-09-22: Usuario corre `pnpm db:seed` y `pnpm fetch:google-photos` con éxito: 30 lugares actualizados, 143 fotos agregadas, 4 sin resultado en Google (Salinas de Pullally, Costanera de Papudo, Paseo El Conquistador, San Lorenzo). El usuario, conocedor del terreno, da alternativas de búsqueda para 3 de los 4: Pullally → laguna/playa Las Salinas; Costanera de Papudo → Playa Grande; San Lorenzo → el puente (más fácil de encontrar en Google que el pueblo). Se agrega `SEARCH_QUERY_OVERRIDES` en `fetch-google-photos.ts` (mapa slug → texto de búsqueda alternativo) para esos 3; queda pendiente Paseo El Conquistador, para el que el usuario dice tener una referencia sacada directo de Google Maps pero no la compartió todavía. `pnpm typecheck`/`lint`/`test`/`format:check` verdes.
- 2026-09-22: Usuario pide reemplazar "Paseo El Conquistador" por Punta Pite (sendero costero entre Papudo y Zapallar diseñado por la paisajista Teresa Moller, 2004-2006, con piscinas naturales y avistamiento de pingüinos de Humboldt). Investigado vía WebSearch (Wikiloc/Wikiexplora/OpenStreetMap/teresamoller.cl bloqueados desde el sandbox, igual que siempre); el usuario provee las coordenadas exactas del acceso desde Google Maps. Se agrega como lugar nuevo `zapallar-punta-pite` (comuna Zapallar, la más citada en las fuentes) y se detecta un gap real en `scripts/seed.ts`: el script nunca borraba lugares sacados del array (solo hace upsert), así que cambiar de slug dejaba huérfano el lugar viejo publicado en la base — se agrega `REMOVED_PLACE_SLUGS` + `removePlaces()`, corrido después de `seedRoutes()` para evitar el `ON DELETE RESTRICT` de `route_stops.place_id` por si alguna ruta lo referenciara todavía. Se renombra la carpeta de staging en `public/fotos/` y se actualiza su README. `pnpm typecheck`/`lint`/`test`/`build`/`format:check` verdes.
- 2026-09-22: Usuario corre el seed y el fetch actualizados: 34/34 lugares con foto, 163 fotos en total, 0 sin resultado, 0 errores. Pregunta por qué no puede navegar entre las fotos nuevas — la ficha seguía mostrando solo la portada (`pickPrimaryPhoto`, posición 0), las demás quedaban guardadas pero sin UI. Se construye el carrusel: `getPlaceBySlug` ahora devuelve `Place.photos` (arreglo ordenado por posición, reemplaza `photoUrl`/`photoAttribution`) y `PlacePhotoHero` pasa a ser client component con flechas y puntos de navegación. Bug evitado al construirlo: el estado interno "la imagen falló" de `PhotoOrIcon` (un `useState` de su `onError`) quedaba pegado entre fotos del carrusel — se agrega `key={foto.url}` para que reinicie con cada foto en vez de arrastrar el fallo de una foto rota a las demás. `pnpm typecheck`/`lint`/`test`/`build`/`format:check` verdes.
- 2026-09-22: Usuario pide que el banner de auspicio de Matichoc "llame la atención" y que el logo real salga sí o sí (antes era una inicial "M" genérica en un círculo). Se revisa `matichoc/matiweb` (repo propio del usuario, clonado localmente) para sacar el logo real (`assets/brand/logo-horizontal.png`, PNG con transparencia) y los colores de marca reales (`css/styles.css`: rosa #d4216c, marrón #64321b/#4b2e2e, amarillo #ffc800, crema #fff8f0) en vez de inventar una paleta. Se copia el logo a `public/brand/matichoc-logo.png` y se agrega `/brand/**` a `images.localPatterns` (mismo tipo de bug que ya se dio con las fotos de Matichoc). Se rediseña `SponsorBanner`: tokens de tema nuevos `--sponsor-foreground`/`--sponsor-accent`/`--sponsor-accent-foreground` en `globals.css` con la paleta real de Matichoc (crema+rosa en claro, café oscuro+rosa en oscuro — el mismo café que usa matiweb en su propio header/footer), logo real visible siempre, borde superior rosa grueso para que destaque. Probado localmente con `next build && next start`: home y el logo cargan 200. `pnpm typecheck`/`lint`/`test`/`build`/`format:check` verdes.
- 2026-09-22: Usuario aclara que quiere comercializar el sitio: vender posiciones publicitarias, no solo un banner fijo de Matichoc. Se pregunta alcance vía AskUserQuestion: elige (1) destacar lugares del listado (no solo el banner) y (2) gestión manual de pagos (sin pasarela). Se implementa el mecanismo base: migración `0009_featured_places.sql` agrega `places.featured_until` (nulo = no destacado; fecha futura = destacado hasta esa fecha, sin pasarela de pago — el dueño del sitio lo carga a mano tras cobrar por fuera). `Place`/`PlaceCard` ganan `isFeatured` (calculado server-side comparando con la fecha actual); `listPlaces` ordena destacados primero; insignia ámbar "★ Destacado" (distinta de las de categoría, para leerse como promoción) en `PlaceCard` y `PlacePhotoHero`, más borde ámbar en la tarjeta. `scripts/seed.ts` soporta un campo opcional `featuredUntil` por lugar (todavía sin usar en ningún lugar del catálogo — falta que el usuario confirme para cuál lugar y hasta qué fecha activarlo, probablemente Chocolatería Matichoc como primer caso). Nota: el panel admin (`/admin/lugares`) sigue siendo un placeholder ("Fase 2"), así que por ahora esto se gestiona igual que todo el resto del catálogo: editando `scripts/seed.ts` y corriendo `pnpm db:seed` — no hay UI de auto-gestión todavía. `pnpm typecheck`/`lint`/`test`/`build`/`format:check` verdes.
- 2026-09-22: Usuario confirma: Chocolatería Matichoc destacada de forma vitalicia (es su propio negocio, no una campaña con fecha de término). Se activa en `scripts/seed.ts` con `featuredUntil: "2099-12-31T23:59:59Z"` — no existe un valor "para siempre" en un campo de fecha, así que se usa una fecha lejana como marcador práctico en vez de agregar una columna/flag separado solo para este caso. `pnpm typecheck`/`lint`/`test`/`build`/`format:check` verdes.
- 2026-09-22: Usuario reporta que Matichoc ya no muestra las 4-5 fotos de Google, solo la curada a mano. Bug real: `pnpm db:seed` borraba TODAS las `place_images` de un lugar y solo reinsertaba la foto curada del seed (`photo` field) — esto ya pasaba antes de esta sesión, pero recién se notó porque ahora hay fotos de Google que también se pierden en cada re-seed. Se extrae `GOOGLE_PLACE_PHOTO_PREFIX` a `scripts/lib/google-photo-prefix.ts` (antes duplicado en cada script) y se corrige `seedPlaces()` para borrar solo filas que NO empiecen con ese prefijo, dejando intactas las fotos que administra `fetch-google-photos.ts`. Ahora ambos scripts son idempotentes sin importar el orden en que se corran. `pnpm typecheck`/`lint`/`test`/`build`/`format:check` verdes.
- 2026-09-22: Usuario (dueño del local) corrige las coordenadas de Chocolatería Matichoc — el nudge aproximado anterior quedaba en otra ubicación. Reemplazadas por las exactas de Google Maps; se limpia la nota "coordenadas aproximadas" de la descripción, ya no aplica. `pnpm typecheck`/`lint`/`test`/`build`/`format:check` verdes.
- 2026-09-22: Usuario pide la siguiente fase grande: mapa de navegación en vivo (auto moviéndose, ruta real por calle, tipo Waze) y, aparte, sugerencias de itinerario multi-día con horarios de apertura y restricciones del usuario. Dado el tamaño y que el mapa en vivo requiere elegir un proveedor de rutas (decisión con costo real de por medio), se pregunta orden y proveedor vía AskUserQuestion: el usuario elige mapa en vivo primero, con Google Routes API (reutilizando el mismo proyecto/facturación de Google Cloud que ya activó para Places).

  Se implementa: `src/app/api/directions/route.ts` (proxy de `computeRoutes`, pide `polylineEncoding: GEO_JSON_LINE_STRING` para no tener que decodificar el polyline codificado de Google a mano — devuelve coordenadas GeoJSON listas para MapLibre); `src/lib/maps/directions.ts` (helper cliente); `MapView` gana dos props opcionales (`routeLine`, `showLiveLocation`) en vez de duplicar el componente — dibuja la ruta real como `Source`/`Layer` de línea y usa el `GeolocateControl` nativo de MapLibre (punto azul que sigue la posición real) para el modo en vivo; `RouteNavigationMap` (nuevo) envuelve `MapView` con un botón "Iniciar/Detener navegación" que calcula la ruta al activarse y cae de forma segura al mapa normal (sin línea de ruta) si la API falla — nunca rompe la página. Se conecta en la ficha de ruta (`/rutas/[slug]`) y en "Mi recorrido" (`/recorrido`), reemplazando el `MapView` estático en ambos.

  Nota real encontrada al tipar: la versión instalada de `maplibre-gl` (5.24.0, fijada a propósito por el bug de v6 documentado antes) no expone `showUserHeading` en `GeolocateControlOptions` — se sacó esa opción, el punto de posición funciona igual sin rotación por rumbo.

  Queda pendiente (no se construyó en esta ronda): habilitar "Routes API" en Google Cloud Console (mismo proyecto que Places — el usuario debe hacerlo, ver `.env.example`); ETA/distancia en vivo actualizada según la posición real del usuario mientras se mueve (v1 solo muestra el resumen total de la ruta calculada, no una distancia restante recalculada dinámicamente); integrar la navegación con el checklist de paradas (marcar automáticamente al acercarse). La segunda mitad del pedido — itinerario multi-día con horarios de apertura y restricciones — queda para una fase aparte, ya elegida por el usuario para después. `pnpm typecheck`/`lint`/`test`/`build`/`format:check` verdes; probado con `next build && next start`: `/api/directions` responde 503 sin API key (esperado, sandbox sin la key) en vez de romper, y las páginas cargan bien.

- 2026-09-23: Usuario pregunta por unas skills de terceros de las que solo tiene el nombre ("UI UX Promax", "claude-mem", "n8n-mcp", "light RAG", "everything Claude Code") — se busca con `SuggestSkills` y no aparece nada con esos nombres en el catálogo disponible para esta sesión; se le informa y se sigue directo con el pedido real de fondo. El usuario describe el problema real: "lo veo como una app del año 2000... nada de brillos, destellos, galaxia y tecnología, nada de diablos por último molestando en la ruta" — pide más viveza visual y que el folclore del diablo (el propio nombre del sitio) se note más. Se agrega, sin herramientas nuevas: `SparkleField` (chispas parpadeantes, `components/ui/sparkle-field.tsx`) y `DevilMascot` (ícono de línea del diablo, mismo lenguaje visual que `CategoryIcon`, `components/ui/devil-mascot.tsx`); nuevos keyframes en `globals.css` (`sparkle-twinkle`, `glow-pulse`, `devil-peek`). El hero del home pasa de un gradiente plano claro a un fondo oscuro tipo "cielo nocturno" (`#1b0e1f` → `#2a1420` → fondo) con halo de luz pulsante, chispas y el diablo asomándose en una esquina — `LocaleSwitcher` se ajusta a texto/borde blancos ya que ahora vive siempre sobre un fondo oscuro (antes dependía del modo claro/oscuro del sitio). En la ficha de "Ruta del Diablo" específicamente (`route.slug === "ruta-del-diablo"`), el diablo se asoma junto al título con la misma animación. `PlaceCard` gana el mismo brillo al pasar el mouse que ya tenía `RouteCard` (glow ámbar si está destacado, terracota si no), por consistencia. Primera pasada de un pedido más grande ("hacer que se vea menos año 2000") — queda abierto a que el usuario redirija qué más destacar (botones, otras páginas, etc.). `pnpm typecheck`/`lint`/`test`/`build`/`format:check` verdes; probado con `next build && next start`: el home carga 200 con las clases nuevas presentes en el HTML.
- 2026-09-23: Usuario despliega el sitio en Vercel exitosamente (screenshot del "Congratulations!" de Vercel) y nota que las tarjetas de "Rutas destacadas" no muestran foto — no era un bug del deploy, `RouteCard` nunca tuvo fotos (a diferencia de `PlaceCard`), solo ícono+gradiente fijo. Se corrige: `listRoutes()` ahora trae la foto de la primera parada de cada ruta (`pickRouteCoverPhoto()`, mismo criterio de "primera por posición" que ya usa `pickPrimaryPhoto`/`sortPhotos` para lugares) y `RouteCard` la muestra igual que `PlaceCard` (con fallback al ícono si no hay foto o falla la carga). El usuario también deja un pedido grande de cuatro partes para después del deploy: (1) mapa tipo Waze interactivo y colorido con íconos alusivos a la zona (dulce de La Ligua, chaleco huaso, el diablo, palta, casco minero, tabla de surf); (2) fichas de lugares/sectores con un estilo tipo TripAdvisor; (3) una función nueva tipo Instagram para que los visitantes posteen/hablen de la zona e interactúen con la ruta — la más grande de las tres, con implicancias de moderación/auth de usuarios reales aún sin definir; y confirma que "convertirlo en app vendible" y "mejoras de seguridad" son procesos continuos, no un solo paso. Queda pendiente priorizar y acotar esas tres piezas con el usuario antes de construir. `pnpm typecheck`/`lint`/`test`/`build`/`format:check` verdes.

  El usuario elige empezar por el mapa temático y las fichas tipo TripAdvisor (dejando lo de Instagram para después, con más definición de producto). Se implementa:

  **Mapa temático:** `getPlaceIcon(categorySlug, placeSlug)` en `lib/ui/category-gradient.ts` — reemplaza el ícono de categoría por uno "de la zona" en lugares puntuales cuyo contenido ya sourceado calza de verdad con el tema (nunca una asignación forzada/inventada): `dulce` → Área de los Dulces de La Ligua (patrimonio cultural inmaterial documentado); `tejido` (chaleco/textil) → Valle Hermoso, "la cuna del tejido"; `diablo` → Escalera del Diablo; `surf` (tabla) → Los Molles y Pichicuy (ambos con ola/surf documentado); `casco-minero` → San Lorenzo (parroquia patrona de mineros) y la línea férrea Cabildo-Pedegua-Petorca (túneles, puente — construida para transporte minero). El ícono de "palta" (aguacate, cultivo emblemático de Petorca) queda agregado a la paleta pero sin asignar: ningún lugar del catálogo lo documenta todavía — pendiente si el usuario quiere sumar un lugar/sector real para eso. Nuevos paths en `CategoryIcon`; se corrige de paso que ningún llamador pasaba el `slug` del lugar (solo la categoría), así que `MapPin`/`MapView`/`PhotoOrIcon` ganan un prop `slug`/`placeSlug` para habilitar el override.

  **Fichas tipo TripAdvisor:** se investiga qué tan real es el sistema de tags (`familiar`/`pet-friendly`/`accesible`/`leyenda`) antes de mostrarlo — resultado: ningún lugar del catálogo tiene tags asignados todavía (`place_tags` nunca se pobló en `scripts/seed.ts`), así que no se inventan etiquetas sin evidencia (mismo criterio que con "leyenda" del "casco minero": solo mostrar lo que ya está sourceado). Se agrega el render de tags a `PlaceCard` de todos modos (no-op hoy, listo para cuando se carguen datos reales) y, con datos reales que sí existen, un badge de cantidad de fotos ("📷 N") en la esquina de la tarjeta cuando hay más de una — toque visual tipo TripAdvisor respaldado en datos reales (`place.photoCount`, nuevo campo) en vez de inventar rating/reviews que no existen. `pnpm typecheck`/`lint`/`test`/`build`/`format:check` verdes.

- 2026-09-26: PR #13 (íconos temáticos + fichas tipo TripAdvisor) mergeado. El usuario propone asignar el ícono de "palta" a la zona de Pedegua, pero de inmediato se corrige a sí mismo: prefiere que la asignación lugar↔ícono deje de estar "afianzada" (hardcodeada) y sea dinámica. Verificación honesta antes de tocar nada: la ficha real de Pedegua en `scripts/seed.ts` documenta la antigua estación/ramal ferroviario, no cultivo de palta — asignarle "palta" habría sido forzado (y Pedegua ya tenía `casco-minero`, real). Se descarta esa opción y se implementa la preferencia del usuario: se retira `PLACE_ICON_OVERRIDES` (mapa fijo en `lib/ui/category-gradient.ts`) y se agrega una columna `places.icon` (nullable, texto libre, sin `CHECK` a propósito — ver migración `0010_place_icon.sql`) que gana sobre el ícono de categoría cuando tiene valor. `getPlaceIcon(categorySlug, placeIcon)` pasa a recibir el ícono ya resuelto en vez de buscarlo por slug; todos los llamadores (`MapPin`, `MapView` incl. popup y `RouteStop` de rutas, `PhotoOrIcon`, `PlacePhotoHero`, `PlaceCard`) se actualizan para pasar el dato en vez del slug. Las 11 asignaciones que antes vivían en el mapa fijo se migran tal cual a `icon:` en cada entrada de `scripts/seed.ts` (mismo criterio real/sourceado de siempre, sin cambios de fondo). "Palta" sigue sin asignar — ahora es tan simple como agregar `icon: "palta"` a un lugar real cuando el usuario nombre uno, sin tocar código de componentes ni redeploy de lógica. Sienta además la base para que el futuro panel de administración (Fase 2, no construido) deje elegir el ícono por lugar desde una UI en vez de editar `seed.ts`. `pnpm typecheck`/`lint`/`test`/`build` verdes.

- 2026-09-26: Viendo el resultado anterior, el usuario pide una evolución más grande para el mapa de `/recorrido`: "un mapa dinámico que pueda ordenar como quiera", que las paradas "se destaquen o brillen al pasar por algún lugar", y que al final "sugiera un lugar en la ruta que esté al paso y no hayas seleccionado". Se le proponen los tres como piezas separadas por costo/riesgo (reordenar a mano primero, brillo de proximidad después, sugerencia de lugares al final — la más difícil, necesita distancia a la línea de ruta real y no es probable de testear en vivo desde este sandbox sin GPS). El usuario responde "dame lo que tengas que dar" — se construyen las dos primeras en esta ronda:

  **Reordenar a mano:** hasta ahora el orden de `/recorrido` siempre lo recalculaba `ItineraryEngine.build` (vecino más cercano), sin memoria de ningún orden elegido por el usuario. Se agrega `ItineraryEngine.buildInOrder` (`lib/itinerary-engine/build-itinerary-in-order.ts`, con pruebas nuevas en `tests/unit/itinerary-engine.test.ts`): arma el itinerario respetando el orden dado tal cual, sin reordenar ni saltarse lugares. `lib/trip/storage.ts` gana un segundo valor en `localStorage` (`petorca-trip-order-mode`, `"auto" | "manual"`) y `reorderTripPlaces`/`resetTripOrder`: el modo por defecto sigue siendo automático (cero cambio de comportamiento si el usuario no toca nada); apenas sube o baja una parada con los botones nuevos (▲/▼) en `TripView`, esa parada pasa a modo manual (aviso en pantalla + botón "Optimizar automáticamente" para volver atrás). Se optó por botones subir/bajar en vez de arrastrar (`drag and drop`): no hay ninguna librería de DnD instalada y el drag nativo de HTML5 no funciona en touch — con botones queda 100% funcional también en el celular en terreno, sin sumar una dependencia nueva para esto.

  **Brillo de proximidad:** `MapView` ahora escucha `onGeolocate` del `GeolocateControl` nativo que ya existía (modo navegación en vivo) y calcula la distancia real (`haversineDistanceKm`, ya usado por el motor de itinerarios) de tu posición a cada parada. Bajo 150 m (~2 min caminando — cerca de verdad, con margen para la precisión típica del GPS) el pin de esa parada pasa a un halo dorado más grande y pulsante (`MapPin`, prop `near`). No se puede probar en vivo desde este sandbox (sin GPS ni acceso a internet real): queda para que el usuario lo prenda en el celular, active "Iniciar navegación" en una ruta o en su recorrido, y se acerque a una parada real para verlo brillar.

  Queda para una ronda aparte (la más difícil de las tres, sin construir todavía): sugerir un lugar del catálogo que quede "al paso" de la ruta real y no esté en el itinerario. `pnpm typecheck`/`lint`/`test` (14, incluidas las nuevas de `buildInOrder`)/`build`/`format:check` verdes.

- 2026-09-26: El usuario pide portadas de ruta "generadas con IA", estilo caricatura/cartel de viaje, mostrando los hitos reales de cada ruta. No hay ninguna herramienta de generación de imágenes disponible en esta sesión de Claude Code — se le explica esa limitación y, en vez de eso, se le escriben tres prompts detallados (uno por ruta) con solo los elementos que ya están sourceados en `scripts/seed.ts`, para que los corra en el generador de IA que él tenga a mano y me mande el resultado — mismo patrón ya usado con el logo de Matichoc y las fotos de Google. Antes de escribir el prompt de "La Ligua y Cabildo", el usuario menciona dos lugares que no están en el catálogo (Puente La Sirena, Castillo del Diablo); se busca en la web antes de incluirlos como si fueran reales: **Puente La Sirena** en Cabildo sí aparece documentado (con su propia leyenda local, "el carretero del puente la Sirena"); **Castillo del Diablo** no aparece ubicado en Cabildo/Petorca en ninguna fuente encontrada (el único real con ese nombre está en Talcahuano, Región del Biobío, sin relación) — se le pregunta directo en vez de inventar, y el usuario confirma que es una leyenda real de pacto con el diablo (casona grande nunca terminada), motivo folclórico que sí existe documentado en otras partes de Chile (Talcahuano, Rancagua, Hualqui) aunque no específicamente en Cabildo — queda anotado como "pendiente de verificación" si se agrega como lugar real, no "verificado", honesto con lo que se pudo confirmar.

  El usuario genera las tres imágenes con esos prompts (afuera, con su propia herramienta de IA) y las manda de vuelta pidiendo que sean las portadas reales de cada ruta. Se implementa la parte de datos: columna nueva `routes.cover_image` (migración `0011_route_cover_image.sql`; el tipo `RouteRow` en `types/database.ts` ya la declaraba desde antes, sin que existiera la columna ni el dato — inconsistencia vieja de la Fase 0 que esto deja resuelta). `Route.coverImageUrl` (ficha de ruta, hero nuevo con `next/image` arriba del título) y `RouteCard.photoUrl` (ya existía, ahora prioriza `cover_image` sobre la foto de la primera parada — `pickRouteCoverPhoto` queda como respaldo). Imágenes guardadas en `public/rutas/<slug>.webp`; `next.config.ts` gana `/rutas/**` en `localPatterns` (misma lección de Next 16 de siempre: hay que declarar cada prefijo local a mano). `scripts/seed.ts` asigna las tres rutas de una vez. `pnpm typecheck`/`lint`/`test`/`build`/`format:check` verdes.

  Queda pendiente, a la espera de que el usuario corrija la propuesta: reordenar/ampliar de verdad los stops de "La Ligua y Cabildo" para que calcen con lo que describió (dulces/chalecos → Valle Hermoso → Chorreada opcional → Cabildo → Castillo del Diablo → Puente La Sirena → zona minera → Alicahue), lo que implica sumar Puente La Sirena y Castillo del Diablo como lugares reales del catálogo — no se tocan los `stopSlugs` todavía, solo se propuso en el chat.

- 2026-09-26: El usuario confirma la propuesta completa ("todas las playas así que sí" / "seguro tiene que estar todo todo eso" / "bien agree a todo") y manda coordenadas exactas para los dos lugares nuevos de Cabildo (Puente La Sirena, Castillo del Diablo). Se implementan las tres rutas:
  - **Ruta Costera**: se suma `zapallar-punta-pite` (ya en el catálogo, sin ruta asignada) entre la bahía de Zapallar y Los Molles.
  - **Ruta del Diablo**: se suma `ruta-de-los-tuneles` (ya en el catálogo) justo después del puente de Pedegua.
  - **La Ligua y Cabildo**: se reescribe completa — `la-ligua-area-de-dulces` → `la-ligua-valle-hermoso` → `la-ligua-la-chorreada` (con nota "parada opcional") → `cabildo-san-lorenzo` → `cabildo-puente-la-sirena` (nuevo) → `cabildo-castillo-del-diablo` (nuevo) → `cabildo-alicahue` → `cerro-chache` → `cabildo-la-vega`. Duración estimada sube de 270 a 360 min.

  Los dos lugares nuevos: **Puente La Sirena** (`cabildo-puente-la-sirena`, coordenadas exactas del usuario, a menos de 100 m de San Lorenzo) queda con fuente real (el video de YouTube "El carretero del puente la sirena. Cabildo." encontrado en la sesión anterior) y `verification_status: "verified"` como el resto del catálogo. **Castillo del Diablo** (`cabildo-castillo-del-diablo`, coordenadas exactas del usuario, ícono `diablo`) queda sin `source` (nada online la ubica en Cabildo específicamente) y con `verification_status: "pending"` — para esto se agregó soporte en `seedPlaces()` para un `verificationStatus` por lugar (antes hardcodeado "verified" para todos) y se hizo opcional el `addSource()` (antes obligatorio para cada lugar).

- De paso se activa un mecanismo que estaba en el esquema desde la Fase 0 pero nunca se usaba: `route_stop_translations.notes` (una nota por parada, por locale). `lib/data/routes.ts` ahora la trae y la mapea a `RouteStop.notes` (antes siempre `null` a mano); `scripts/seed.ts` deja que un stop sea un slug simple o `{ slug, notes: { es, en } }`; `RouteStopChecklist` la muestra en cursiva bajo el nombre de la parada. Usada hoy solo para marcar "La Chorreada" como parada opcional en la nueva Ruta Patrimonial.

- 2026-09-26: El usuario pide "ir sumando lineamientos al repositorio" para que una página nueva siga la misma línea estética, y que un cambio nuevo "se replique entre todas las páginas" en vez de tener que aplicarlo página por página. Se audita lo que ya existe (no se inventa un sistema de diseño desde cero) — `globals.css` (tokens de color + `@keyframes`/`--animate-*`), `lib/ui/category-gradient.ts` (color/gradiente/ícono por categoría), `ICON_PATHS` en `category-icon.tsx`, y los patrones repetidos entre `PlaceCard`/`RouteCard`/badges (radios, glow de hover, posición fija de insignias) — y se documenta en `docs/DESIGN.md` (mismo estilo que `docs/ARCHITECTURE.md`/`docs/DATA-MODEL.md`): tokens, cuándo usar cada animación, la regla de "un solo lugar de verdad por tipo de token" (nunca un color/ícono/keyframe suelto dentro de un componente puntual) y la regla ya aplicada varias veces en esta sesión de no fabricar íconos/tags/leyendas sin contenido real detrás. Se agrega `@docs/DESIGN.md` a `CLAUDE.md` (junto al `@AGENTS.md` que ya había) para que cualquier sesión de Claude Code sobre este repo lo cargue automático, sin que el usuario tenga que pedirlo de nuevo cada vez.

  `pnpm typecheck`/`lint`/`test` (14)/`build`/`format:check` verdes. Falta que el usuario corra `supabase db push` + `pnpm db:seed` localmente para aplicar todo (migraciones 0010-0011, las nuevas paradas y notas).

- 2026-09-26: Al intentar aplicar las migraciones, `supabase db push` se queda pegado en "Connecting to remote database..." (problema de red/CLI en la máquina del usuario, no del código) — se le sugiere pegar el SQL directo en el SQL Editor del dashboard como alternativa. Ahí aparece un error real: `column "cover_image" of relation "routes" already exists`. Error mío: `routes.cover_image` ya existía desde `0004_routes.sql` (Fase 0) — asumí, sin revisar esa migración, que el campo declarado en `RouteRow` (`types/database.ts`) no tenía columna real detrás, cuando en realidad sí la tenía desde siempre (lo que sí faltaba era poblarla y usarla en el código, no crearla). Como el SQL Editor corre las sentencias en una transacción implícita, el error de `cover_image` también deshizo el `add column icon` de la línea anterior. Se corrige `0011_route_cover_image.sql` a `add column if not exists` (no-op donde ya existe, crea la columna en un proyecto nuevo que no la tenga) y se le indica al usuario correr solo el `alter table places add column icon text;` para desbloquearse ahora. Ningún dato ni código de `cover_image`/`RouteCard`/hero de ruta estaba mal — el problema era solo la migración de más.

- 2026-09-26: `pnpm db:seed` corre limpio en la máquina del usuario (36 lugares, 3 rutas). Dos correcciones más sobre lo recién sembrado:
  - El usuario corrige el mapa de la Ruta Costera: la parada `papudo-pullally` decía "Salinas de Pullally", pero esa ubicación (mismas coordenadas de siempre) es la **Laguna Pullally** — son dos lugares reales distintos del mismo humedal. Da además las coordenadas reales de las Salinas (`-32.41007831226611, -71.41062008860503`). Se renombra `papudo-pullally` a "Laguna Pullally" y se agrega `papudo-salinas-pullally` como lugar nuevo en esas coordenadas; ambos citan la misma fuente real (Wikipedia — Humedal Salinas de Pullally y Dunas de Longotoma), honesto porque los dos son parte real de ese mismo santuario protegido. Luego el usuario aclara que la Laguna no debe ser parada de la ruta (solo un lugar más en `/explorar`/`/lugares`) — se saca `papudo-pullally` de `stopSlugs` de "Ruta Costera: Papudo y Zapallar", dejando ahí solo las Salinas.
  - De paso, pide que `/explorar` use una cuadrícula más chica "parecido a recuadros de carpetas" en vez de tarjetas grandes. `PlaceCard` (se verificó que solo se usa ahí antes de tocarlo) pasa de tarjeta rica (foto 16:9, descripción, tags, insignia de verificación en texto) a ficha compacta cuadrada: miniatura `aspect-square`, nombre y comuna en dos líneas, sin descripción/tags/categoría en texto (el gradiente/ícono ya distingue la categoría). La insignia "Pendiente de verificación" se saca del todo (estado por defecto, puro ruido en una grilla chica); "Verificado" queda como un check discreto. Grilla de `/explorar`: `grid-cols-2` a `grid-cols-6` según ancho. Tests de `PlaceCard` actualizados a la nueva UI.

  Se completa además `/informacion`, hasta ahora un título vacío: se agrega una descripción corta del sitio y un crédito de desarrollo ("Desarrollo de Cristóbal Roco, CTO de Matichoc", tal cual lo pidió el usuario) en un namespace `info` nuevo en `messages/es.json`/`en.json`. No se agregó ningún correo/teléfono de contacto — el usuario no dio uno específico para publicar, y no correspondía inventar un canal.

  `pnpm typecheck`/`lint`/`test` (14)/`build`/`format:check` verdes.

- 2026-09-26: `pnpm db:seed` corre limpio en la máquina del usuario (37 lugares). El usuario trae una lista larga de seis puntos "para poner en plan" — se responde cada uno sin tocar código todavía salvo donde ya había suficiente claridad:
  1. **Íconos**: el usuario manda captura de los pines del mapa y aclara qué quería de verdad — no elegir el ícono de un _lugar_ (eso ya existe, es automático vía `place.icon`), sino elegir una **ficha propia** (tipo Monopoly) que lo representa a _él_ en el mapa y que se mueve con su ubicación real en vivo, en vez del punto azul genérico. Se construye: `lib/navigation/avatar-storage.ts` (localStorage, mismo patrón que `lib/trip/storage.ts`, elige entre los 6 íconos temáticos: diablo/dulce/tejido/surf/casco-minero/palta), `AvatarPicker` (selector junto al botón "Iniciar navegación") y `PlayerToken` (la ficha dibujada en el mapa); `MapView` pasa `showUserLocation={false}` al `GeolocateControl` nativo para que la ficha sea la única marca de posición.
  2. **Mapa de navegación estático** ("no se puede ver"): se sospechaba que faltaba habilitar Routes API — el usuario manda captura confirmando que ya está habilitada. Se descarta esa causa; como no se puede reproducir la llamada real desde este sandbox (sin acceso a `routes.googleapis.com`), se mejora `/api/directions` para que loguee el cuerpo real del error de Google en los logs del servidor (antes se perdía) y se le pide al usuario revisar **restricciones de la API key** en Google Cloud Console (habilitar la API en el proyecto no es lo mismo que permitirla en la llave) como sospechoso más probable.
     3 y 4. **Mística en todo el sitio / brillos "mágicos"**: el usuario responde directo — "todo el sitio debe conservar la mística de la zona", no solo el home. Esto reemplaza la regla anterior de `docs/DESIGN.md` ("el hero del home es la única superficie oscura"). Se crea `PageHero` (`components/ui/page-hero.tsx`): la misma cabecera oscura con `SparkleField` + halo `glow-pulse` del home, como tarjeta contenida en vez de a todo el ancho, y se reemplazan los `<h1>` sueltos de `/explorar`, `/rutas`, `/recorrido` e `/informacion`. Las páginas con foto/portada propia (ficha de lugar, ficha de ruta) no la llevan encima, para no recargar. `ViewToggle` en `/explorar` queda fuera del `PageHero` (sus colores están pensados para fondo claro, se verían mal sobre el fondo oscuro sin adaptarlo primero).
  3. **Costo de Google Maps**: respondida con cifras reales de la documentación oficial de Google (cambio de modelo de precios de marzo 2025: ya no hay crédito plano de US$200/mes, cada API tiene su propia cuota gratis — Maps JavaScript API da 10.000 cargas gratis/mes, luego ~US$7/1.000). A la escala actual del sitio, probablemente sale gratis.
  4. **Sumar alojamiento/restaurantes/supermercados/bencineras/salud al mapa**: triage por fuente real — restaurantes/supermercados/bencineras/hospitales/CESFAM son factibles ahora con la misma Google Places API que ya se usa (Nearby Search); Airbnb no tiene API pública (solo cabría un botón que abra Airbnb con las coordenadas); Booking.com requiere aplicar como partner; TripAdvisor cobra por su API de contenido. Sin construir todavía — el usuario no ha priorizado esto aún.

  El usuario sigue con una pregunta nueva a medio camino: "quiero que el mapa se vea parecido a Maps o Waze" — se le explica que MapLibre (lo que usamos) es solo el renderizador; el look "tipo Google Maps" depende del estilo de tiles vectoriales que se le pase (`NEXT_PUBLIC_MAP_STYLE_URL`, ya existe la variable), y se recomienda un estilo tipo MapTiler "Streets" (gratis hasta ~100k cargas/mes) en vez de saltar directo a la API paga de Google Maps — pendiente de que el usuario cree una cuenta y dé la key, no se implementó todavía.

  `pnpm typecheck`/`lint`/`test` (14)/`build`/`format:check` verdes.

- 2026-09-26: El usuario prueba el PR anterior en el celular y manda cinco capturas con feedback nuevo:
  - **Íconos de la ficha ("Tu ficha:")**: se ven poco claros a simple vista y no tienen tooltip al pasar el mouse. Pregunta si hay "otro MCP o API para íconos gratis" — se le responde que no hace falta ninguno: son SVG estáticos empaquetados en el sitio (sin costo, sin llamada de red); se podría adoptar una librería de íconos abierta (ej. Lucide, MIT) para los 4 íconos genéricos de categoría, pero los 6 temáticos de la zona (diablo, dulce, tejido, surf, casco-minero, palta) son conceptos demasiado específicos para existir en cualquier librería — necesitan arte propio de todos modos. Se agrandan los botones del selector (`h-7`→`h-10`, ícono `h-3.5`→`h-5`) y se agrega `title`/`aria-label` con el nombre real de cada ficha en español/inglés (`route.avatarIcons.*` nuevo en `messages/es.json`/`en.json`) — así al menos se lee qué es cada una al pasar el mouse; el trazo de los íconos en sí queda igual por ahora, a la espera de que el usuario diga cuáles puntualmente siguen sin entenderse una vez que se vean más grandes y con nombre.
  - **Mapa de navegación sigue sin calcular ruta por calle**: sigue fallando después del fix de logging del PR anterior — pendiente de que el usuario revise el log de Vercel (Runtime Logs de la función `/api/directions`) y lo comparta, no se puede seguir diagnosticando a ciegas.
  - **`/informacion`**: se agregan los datos de contacto de Cristóbal que dio el usuario — Instagram `@el.cristo.cl`, WhatsApp `+56942586908` (link `wa.me`), y su frase "Apoya tus proyectos de IA y con IA." Los de Matichoc quedan pendientes: el usuario pidió sumarlos pero no dio los handles reales (`NEXT_PUBLIC_SPONSOR_INSTAGRAM_URL` en `.env.example` sigue vacío a propósito, "no inventamos una cuenta sin verificar") — hay que pedírselos.
  - **Logo de Matichoc**: el usuario manda "logo 1" (el principal, vertical, vainilla de cacao + tableta + chispas doradas) para reemplazar el "logo 2" horizontal que se estaba usando en `SponsorBanner`. Se reemplaza `public/brand/matichoc-logo.png` por `matichoc-logo.webp` (2000×2000, sin necesidad de convertir formato — Next.js sirve webp nativo) y se sube un poco el alto mostrado (`h-11`→`h-14`, `sm:h-14`→`sm:h-16`) porque el logo nuevo es cuadrado con el nombre incluido abajo, y a una altura muy chica ese texto se pierde.
  - **Fichas de ruta (pendiente, no construido en esta ronda)**: pide que cada parada en la lista tenga una descripción corta + una foto (ya existen como datos reales — `shortDescription`/`place_images` — solo falta traerlos a `RouteStop` y mostrarlos), y que el mapa y la lista se resalten entre sí al pasar el mouse por uno u otro, para orientarse mejor. Se le confirma que es una funcionalidad real y bien acotada, propuesta para la siguiente ronda dedicada en vez de meterla de apuro en esta.

  `pnpm typecheck`/`lint`/`test` (14)/`build`/`format:check` verdes.
